# OmniMath — data-flow audit

AUDIT METHOD: read /Users/sbkoth/work/Omni/OmniMath/docs/REGULATORY.md, /Users/sbkoth/work/Omni/OmniMath/ISSUES.md, /Users/sbkoth/work/Omni/OmniMath/docs/PRIVACY.md, /Users/sbkoth/work/Omni/OmniMath/docs/APP_STORE.md, /Users/sbkoth/work/Omni/OmniMath/Info.plist, and grepped all Swift sources for network, SDK and sensor APIs. Where docs and code disagreed I trusted the code.

WHAT THE CODE ACTUALLY SHOWS:

1. NO ACCOUNTS, NO SERVER OF OURS. No Sign in with Apple (`ASAuthorization` — 0 hits), no CloudKit/`CKContainer` (0 hits), no entitlements file exists anywhere in the repo (`find . -name "*.entitlements"` → empty; no `CODE_SIGN_ENTITLEMENTS` in project.pbxproj), so there is no iCloud sync, no push, no app groups. Prameya operates no backend that receives anything from this app.

2. NO APP-AUTHORED NETWORKING. `URLSession`, `NWConnection`, `Network.` → 0 hits in app sources. The ONLY egress is from the two vendored Google binaries in /Users/sbkoth/work/Omni/OmniMath/ThirdParty/GoogleAdsSPM: `GoogleMobileAds.xcframework` (Google Mobile Ads SDK 12.14.0) and `UserMessagingPlatform.xcframework`.

3. ADS RUN AT EVERY LAUNCH, UNCONDITIONALLY. /Users/sbkoth/work/Omni/OmniMath/OmniMathApp.swift calls `await AdConsent.requestConsentAndStartAds()` in a root `.task`. `AdMobConfig.useProductionAds = true`, so production banner (`ca-app-pub-6659943038442923/3356417926`) and interstitial (`.../7323618486`) units serve in Debug and Release. There is no build flag or user setting that turns ads off. The launch order in /Users/sbkoth/work/Omni/OmniMath/Utilities/AdConsent.swift is: UMP consent info + form → ATT request (if `.notDetermined`) → `MobileAds.shared.start()` → preload interstitial. Interstitial cadence: every 2 chapter completions, 90-second cooldown (`AdMobConfig`, `InterstitialPolicy`).

4. WHAT THE AD SDKs COLLECT — read from the frameworks' OWN privacy manifests, not from memory. `GoogleMobileAds.framework/PrivacyInfo.xcprivacy` declares collection of: Device ID (linked to user, `Tracking = true`), Advertising Data (linked), Coarse Location (linked), Product Interaction (linked), Performance Data, Crash Data, Other Diagnostic Data — purposes Third-Party Advertising, Developer Advertising, Analytics. `UserMessagingPlatform.framework/PrivacyInfo.xcprivacy` declares Coarse Location, Performance Data, Product Interaction (all unlinked, App Functionality). NOTE: coarse location is declared as collected and LINKED by Google even though the app itself uses no CoreLocation (`import CoreLocation` → 0 hits) — it is derived from IP.

5. ATT AND SKADNETWORK. Info.plist carries `NSUserTrackingUsageDescription` and ~50 `SKAdNetworkItems` identifiers. `ATTrackingManager.requestTrackingAuthorization()` is called at launch whenever status is `.notDetermined` — it is not optional or conditional on a setting.

6. AD POLICY POSTURE (/Users/sbkoth/work/Omni/OmniMath/Utilities/AdPolicy.swift). `maxContentRating = .general` (G cap requested). `childDirectedTreatment = .unset` and `underAgeOfConsent = .unset` — deliberately NOT `false`, because `false` is an affirmative certification to Google the owner has not made. `AdConsent.applyPolicy()` only assigns the SDK properties when `boolValue != nil`, so nothing is transmitted for those tags. A report-an-ad affordance exists (/Users/sbkoth/work/Omni/OmniMath/Views/AdReportView.swift) — a `mailto:` to admin@prameya.legal containing only typed text plus app version/build, plus a link to Google Ad Settings.

7. NO MLX, NO MODEL DOWNLOAD, NO HUGGING FACE. Confirmed: `huggingface` → 0 hits; `import CoreML`/`import Vision` → 0 hits; no model weights in the repo. /Users/sbkoth/work/Omni/OmniMath/Domain/Intelligence/InferenceProvider.swift is a protocol seam whose own header says "THERE IS NO MLX IN THIS APP TODAY, AND THIS FILE DOES NOT ADD ANY", and its on-device path returns an unavailable error. ISSUES.md records "On-device model: None. No MLX, no CoreML, no model weights anywhere". So the portfolio-wide Hugging Face model-download disclosure DOES NOT APPLY to OmniMath, and asserting it would be false.

8. NO SENSORS OR PERSONAL-DATA APIs. `HKHealthStore`/HealthKit, `AVCapture`, `PHPicker`/PhotosUI, Speech, Contacts, ClassKit, StoreKit → 0 hits. Info.plist contains no camera/photos/mic/location purpose strings — the only purpose string is `NSUserTrackingUsageDescription`. No in-app purchase, no user-generated content, no uploads.

9. ON-DEVICE STORAGE, EXHAUSTIVE. `UserDefaults` only, four keys: `omnimath.completedLessons`, `omnimath.stars`, `omnimath.lastChapter` (/Users/sbkoth/work/Omni/OmniMath/Models/ProgressStore.swift) and `hasSeenOnboarding` (`@AppStorage` in OmniMathApp.swift). A working "Reset All Progress" control exists in /Users/sbkoth/work/Omni/OmniMath/Views/ProgressView.swift calling `progress.resetAll()`.

10. KNOWN GAPS STILL OPEN AT AUDIT TIME (Wave 2 targets, all recorded in ISSUES.md): no app-target `PrivacyInfo.xcprivacy` (MATH-002 — the only manifests present are inside the vendored Google frameworks); no in-app Settings screen and therefore no in-app privacy-policy link (MATH-026); no UMP privacy-options re-consent entry point — `presentPrivacyOptionsForm` → 0 hits (MATH-009); `ITSAppUsesNonExemptEncryption` absent from Info.plist (MATH-003); `app-ads.txt` not hosted (MATH-012). The policy text describes the intended post-Wave-2 state for the two user-facing ones (in-app policy link, ad-privacy-choices re-consent) and marks them TO VERIFY BEFORE PUBLISHING.

11. NO CONSUMER HEALTH DATA. Nothing in the app touches health, wellness, biometrics, or bodily functions; REGULATORY.md §7.3 expressly rules out MHMDA, Nevada SB 370 and the FTC Health Breach Notification Rule for this app. `healthPolicyMarkdown` is therefore an empty string.

12. DISTRIBUTION. iPhone only (`TARGETED_DEVICE_FAMILY = 1`), iOS 17.0 minimum, category Education, availability "All territories" per docs/APP_STORE_LISTING.md — so GDPR/UK GDPR are live and the policy covers them.

## Corrections to prior claims

- CLAIM (docs/PRIVACY.md, 'Advertising' section): 'If ads are enabled in your build of the App…'. FALSE AS FRAMED. Ads are not conditional. `AdMobConfig.useProductionAds = true` and OmniMathApp.swift calls `AdConsent.requestConsentAndStartAds()` in the root `.task` on every launch. There is no build, setting or user choice in which OmniMath does not show ads. ACCURATE: 'OmniMath displays banner and interstitial ads through Google AdMob. Ad loading begins when you open the app.'
- CLAIM (docs/PRIVACY.md, Summary): 'Where you allow it, the App may request App Tracking Transparency permission.' MISLEADING. The ATT request is unconditional whenever status is `.notDetermined` — `AdConsent.requestTrackingAuthorizationIfNeeded()` runs at launch on every fresh install. ACCURATE: 'The first time you open OmniMath, iOS shows Apple's tracking permission dialog.'
- CLAIM (docs/PRIVACY.md, Children's privacy): 'We do not knowingly collect personal information from children under 13 on our own servers.' TECHNICALLY TRUE BUT EMPTY AND THEREFORE DECEPTIVE IN EFFECT. Prameya operates no servers at all, so the sentence excludes exactly the collection that actually happens — Google's ad requests, which fire at launch before any age question. ACCURATE: say that we collect nothing directly because we have no server, AND that the advertising identifier can be transmitted to Google for any user including a child, AND give parents the concrete controls (Tracking setting, Screen Time).
- CLAIM (docs/PRIVACY.md, Data sharing and sale): 'We do not sell your personal information.' INCOMPLETE, AND INCOMPLETE IN THE DIRECTION THAT FAVOURS US. It omits 'sharing' for cross-context behavioural advertising, which is a separate defined term under CPRA and is exactly what happens when ATT is authorised. ACCURATE: 'We do not sell personal information for money. When you allow tracking, an advertising identifier is disclosed to Google for personalised advertising; California law calls that sharing for cross-context behavioural advertising, and some state laws would call it a sale. The opt-out is to deny tracking.'
- CLAIM (docs/APP_STORE.md §5 label table): 'Everything else (contacts, health, LOCATION, financial, browsing, purchases, user content) — Not collected.' FALSE for location. GoogleMobileAds.framework/PrivacyInfo.xcprivacy declares `NSPrivacyCollectedDataTypeCoarseLocation` with `Linked = true`, for third-party advertising and analytics. ACCURATE: coarse location IS collected by the ad SDK — derived from IP address, not from GPS — and linked to the user. The app itself uses no CoreLocation and requests no location permission; both facts must be stated together.
- CLAIM (docs/APP_REVIEW_INFORMATION.md, lines 127-129): 'App privacy nutrition labels in App Store Connect have been updated to disclose that data may be used to Track You.' UNSUBSTANTIATED. ISSUES.md MATH-010 records that this was never re-derived from the actual dependency graph. ACCURATE: the labels must be derived from the two vendored framework manifests before each submission and the derivation recorded. A label asserted from memory is both an App Review problem and an FTC Act §5 problem.
- CLAIM (portfolio baseline, recorded in ISSUES.md §H): 'No third-party SDKs that transmit' and 'App Privacy answer is Data Not Collected' and '`NSPrivacyTracking = false`'. ALL THREE ARE FALSE FOR OMNIMATH. It vendors Google Mobile Ads 12.14.0 and the User Messaging Platform as binary xcframeworks, it transmits on every launch, it cannot answer 'Data Not Collected', and `NSPrivacyTracking` must be `true` with Google's tracking domains listed. Any shared policy or shared manifest template applying the baseline to OmniMath is wrong. (TO VERIFY: I could not fetch the live text at prameyallc.github.io/edu-app-privacy, so confirm the exact wording of any portfolio-wide 'no networking / no third-party SDK' assertion before relying on this correction verbatim.)
- CLAIM (portfolio baseline, recorded in ISSUES.md §H): 'Inference runs on-device via MLX.' FALSE FOR OMNIMATH — and this is the one app where the portfolio-wide Hugging Face model-download disclosure MUST NOT be copied in. There is no MLX, no CoreML, no model weights, no `huggingface` reference anywhere in the repo. Domain/Intelligence/InferenceProvider.swift is an inert protocol seam whose own header says so and whose on-device path returns an unavailable error. ACCURATE: OmniMath contains no model, downloads none, and performs no inference. Writing a Hugging Face paragraph into this app's policy would itself be a false statement.
- CLAIM (the existing shared edu-app-privacy policy's stated scope: apps with no accounts, no uploads, no health or financial data). ACCURATE FOR OMNIMATH on all three limbs — this app genuinely has no accounts, no uploads and no health or financial data — but the shared policy is nonetheless inadequate here because it was never scoped for an ad-supported app: it lacks CCPA/CPRA sharing disclosure, GDPR legal bases and rights, retention, the coarse-location disclosure, SKAdNetwork, and the children's-plus-advertising analysis. Do not resolve this by pointing OmniMath at the shared policy.
- CLAIM (docs/PRIVACY.md canonical URL block): 'Canonical public URL (App Store Connect): https://github.com/prameyallc/edu-app-privacy/blob/main/PRIVACY.md'. STALE AND UNSUITABLE. ISSUES.md MATH-023 records it as a code-host blob URL subject to GitHub chrome and login walls. ACCURATE: https://prameyallc.github.io/privacy/omnimath/ — and the same correction must be made in README.md, docs/ADMOB_SETUP.md §, docs/APP_STORE_LISTING.md and docs/APP_REVIEW_INFORMATION.md, all of which repeat the old URL.
- CLAIM (docs/PRIVACY.md, final line): 'Apple "User Privacy Choices URL" may point to this same document or to a dedicated choices section on your website when available.' NOT A PRIVACY CLAIM AT ALL — it is an internal instruction to the developer left inside a user-facing policy. Remove it from anything published.
- CLAIM (docs/PRIVACY.md, Information stored on your device): the list omits `hasSeenOnboarding` and `omnimath.lastChapter`. Minor, but the policy presents itself as a complete list. ACCURATE: four keys — completed chapters, Insight Stars, last visited chapter, onboarding-seen flag.
- OMISSION (docs/PRIVACY.md): no mention that the app ships ~50 SKAdNetwork identifiers in Info.plist for ad-install attribution. Not harmful, but it is a real off-device measurement mechanism and belongs in an accurate disclosure.
- OMISSION (docs/PRIVACY.md): no GDPR/UK GDPR section at all, despite 'All territories' availability in docs/APP_STORE_LISTING.md; no legal bases, no controller identification, no supervisory-authority complaint route, no retention statement, and no CCPA rights beyond a single sentence. For an ad-supported app distributed in the EEA and UK these are not optional.

## Open questions

- IN-APP PRIVACY LINK (MATH-026): the shipping build has no Settings screen at all, so there is no in-app link to this policy. App Review Guideline 5.1.1(i) expects the link inside the app. Confirm the Wave 2 Settings screen has landed before publishing, and before submission.
- UMP RE-CONSENT (MATH-009): `presentPrivacyOptionsForm` appears nowhere in the repo. The policy says consent can be withdrawn as easily as it was given, which is a GDPR Art. 7(3) requirement. That sentence is not yet true of the shipping build. Ship the 'Ad privacy choices' entry point or amend the paragraph.
- APP PRIVACY MANIFEST (MATH-002): there is no `PrivacyInfo.xcprivacy` for the OmniMath app target — the only ones present are inside the vendored Google frameworks. It must declare the `UserDefaults` required-reason (CA92.1), `NSPrivacyTracking = true`, Google's current tracking domains, and the collected data types. App Store Connect rejects the upload without it. Take the tracking-domain list from Google's own page for SDK 12.14.0; do not guess it.
- APP STORE PRIVACY LABELS vs GOOGLE'S MANIFEST: docs/APP_STORE.md §5 says location is 'Not collected', but GoogleMobileAds' own manifest declares Coarse Location as collected AND linked to the user. Re-derive every label answer from the two framework manifests before submission, and correct the location answer if the labels currently say otherwise.
- CHILD-DIRECTEDNESS DETERMINATION (MATH-006 / OQ-M04): the children's section of this policy rests on OmniMath being a general-audience app. There is still no dated, retained intended-audience determination anywhere in the repo. Record it, with an author and a date, and re-affirm it whenever store copy changes. The 2025 COPPA amendments expressly weigh marketing materials, representations to consumers, and reviews.
- AD-FREE OPTION: the policy states there is no paid ad-free version. Confirm that is still the intended shipping configuration — if an ad-free purchase is ever added, this policy needs rewriting, not patching.
- APP-ADS.TXT (MATH-012) is not hosted. Not a privacy-policy issue, but the developer website declared in App Store Connect is the same domain that will host this policy — decide the domain once and host both there.
- UK ICO CHILDREN'S CODE (OQ-M09): its scope test is whether the service is 'likely to be accessed by' UK under-18s, which is a lower bar than COPPA's 'directed to children'. An ad-supported maths education app is plausibly in scope. This policy does not claim Children's Code conformance. Counsel should decide whether conformance work is needed for UK distribution.
- TEXAS SB 2420 / DECLARED AGE RANGE (MATH-008): not implemented. If Apple's age-signal APIs are adopted, the app will receive an age category from Apple, which is itself a new data flow and would need a line in this policy.
- CCPA APPLICABILITY: the policy honours CCPA rights while noting Prameya may fall below the statutory thresholds. Counsel should confirm whether that hedge is the position the owner wants stated publicly, or whether to drop the sentence and simply honour the rights.
- PRIVACY POLICY URL MIGRATION (MATH-023): App Store Connect, AdMob, README.md, docs/ADMOB_SETUP.md, docs/APP_STORE_LISTING.md and docs/APP_REVIEW_INFORMATION.md all still point at github.com/prameyallc/edu-app-privacy/blob/main/PRIVACY.md. All six need updating to https://prameyallc.github.io/privacy/omnimath/ at the same time, or the App Store listing will link to a policy that contradicts this one.
- docs/PRIVACY.md IN THIS REPO must be replaced with this text (or reduced to a pointer). Leaving the July 2026 draft in place means two policies with different contents exist for the same app.